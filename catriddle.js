function findcat(n) {
   const boxes = [1, 2, 3, 4, 5];
   let catfound = false;
   for (let i = 0; i <= boxes.length; i++) {
      let currentBox = Math.floor(Math.random() * boxes.length);
      if (currentBox === boxes[0] || currentBox === boxes[boxes.length]) {
      }
   }
}
